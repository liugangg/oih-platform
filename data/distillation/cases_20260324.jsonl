{"input":"ProteinMPNN designed_chains=['A'] 对所有靶点","reasoning":"RFdiffusion binder_design模式输出PDB中，chain顺序取决于输入PDB。HER2原始是chain C，RFdiff输出后binder=A(短链), target=B(长链)。但CD36/EGFR/PDL1等原始chain A的靶点，RFdiff输出后target=A(长), binder=B(短)。MPNN硬编码chains_to_design='A'导致设计了target蛋白而不是binder。273次MPNN全部设计了错误的链。","action":"pipeline.py用gemmi动态检测最短链作为binder_chain，传给ProteinMPNN。不再硬编码'A'。","outcome":"修复后重跑5个靶点。HER2数据有效（chain A恰好是binder）。验证方法：MPNN FASTA original序列长度应是60-100aa(binder)不是几百aa(target)。commit c14eeee"}
{"input":"旧任务在API重启后自动恢复执行，GPU被orphan AF3进程占满","reasoning":"task_manager._load_history()从JSON文件加载任务状态。虽然running任务被标记为failed，但pipeline coroutine内部的子任务（AF3等）已经在docker容器内执行，不受API重启影响。docker restart不杀容器内子进程，docker stop也不一定杀GPU进程。","action":"1. docker kill（非stop/restart）杀容器+所有子进程 2. 手动修改所有task JSON status为cancelled 3. 确认nvidia-smi无GPU进程后再启动API","outcome":"需要docker kill+改JSON文件+确认GPU清空的完整三步才能彻底清理"}
