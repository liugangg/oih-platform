# 2026-03-16 Session Log

## 今日目标
1. 前端优化：dashboard.html 真实任务端到端测试 + 动态Tab解锁验证
2. bioRxiv 论文写作启动

## 系统状态
- API: running (8080), 11容器全部up
- 100ns MD: 后台运行中 (5XWR+aspirin)
- sync脚本: 已同步3个文件

## 待确认
- dashboard 动态Tab是否随任务进度解锁
- MD当前阶段(EM/NVT/NPT/MD)

## 注意事项
- 前端修改后需要重新serve dashboard.html
- Paper数据来源：5XWR+aspirin pipeline结果 + ADC工具链测试数据

---

## 已完成

### 1. PTM检测 + 工具输入生成（qwen_agent.py）
- `detect_ptm(pdb_content)`: 解析PDB，检测糖基化(NAG/FUC/MAN等)、磷酸化(SEP/TPO/PTR)、二硫键(SSBOND)、链序列
- `generate_tool_inputs()`: 根据检测结果自动生成 af3_input.json / gromacs_ptm_notes.json / adc_input.json
- `AgentChatRequest` 追加4个Optional字段：pdb_content/fasta_sequence/smiles/filename
- 附件处理在 agent.chat() **之前**，拼接 context 到 message，20轮循环完全不改
- 生成文件路径: `/tmp/oih_uploads/{session_id}/`
- 测试验证：纯文本调用200正常，SMILES调用自动调chemprop，PDB上传正确生成3个JSON文件

### 2. is_simple_message 动态 thinking_budget
- `QwenBioAgent.is_simple_message()`: 短消息(<20字符) + 匹配寒暄词 → skip_skills=True, budget=0
- 效果：`hi` → budget=0（秒回），`预测ADMET` → budget=1024+（完整推理）
- 同时跳过 skills_loader 注入，减少 system prompt 长度

### 3. skills/PTM_UPLOAD_PARADIGM.md
- Qwen PTM决策范式：5条规则 + 2个典型workflow示例
- skills_loader.py 关键词: 上传/upload/pdb文件/fasta/smiles/糖基化/磷酸化/二硫键/ptm/修饰/modification

### 4. oih-api.service 陷阱
- 发现 systemd oih-api.service (enabled) 与 nohup 手动启动冲突
- kill 后 systemd 自动拉起旧进程，导致新代码不生效
- 解决：重启前先 `systemctl stop oih-api`
- 已追加到 CLAUDE.md

### 5. sync脚本同步
- `python scripts/sync_claude_to_skills.py --apply` → 3个文件更新（GROMACS skill + router + qwen_tools.py）

### 6. dashboard.html 附件上传UI（794→959行，+165行）
- 📎 按钮 + 弹出菜单（PDB上传/FASTA粘贴/SMILES粘贴）
- 附件标签预览（输入框上方，📄/🧬/💊 图标，可 × 删除）
- PTM 检测结果卡片（解析 assistant 回复，✅/⚠️/📁 分类渲染）
- 粘贴浮层（FASTA textarea / SMILES input）
- doSend 扩展：POST body 携带 pdb_content/fasta_sequence/smiles/filename
- 发送后自动清除附件状态

### 7. 100ns MD 状态确认
- 5XWR+aspirin 100ns MD：22.74ns/100ns（22.7%），自然退出
- 轨迹 24GB (`md.xtc`)，113,687 帧，性能 19.2 ns/day
- 进程已 kill，可从 checkpoint 续跑

### 8. CLAUDE.md 更新
- 追加 2026-03-16 完成项（前端/后端/知识文件/测试/待完成）
- sync 脚本同步（0 个文件需更新，已是最新）

---

## 收工状态
- API: running (8080)
- 11容器全部 running
- GROMACS mdrun 已停止（22.74ns）
- CLAUDE.md + session log 已同步
- dashboard.html 已更新但未重新 serve（下次启动时生效）

## 下次待做
- [ ] Case 2：纳米抗体设计实验（Fig 4 数据）
- [ ] Case 3：HER2-ADC 设计实验（Fig 5 数据）
- [ ] Fig 6：各工具运行时间统计
- [ ] MD 动画渲染（MDAnalysis → gif）
- [ ] 100ns MD 从 22.74ns checkpoint 续跑
- [ ] MM/PBSA 结合自由能计算
- [ ] 端到端真实任务测试：验证 dashboard 动态 Tab 解锁
