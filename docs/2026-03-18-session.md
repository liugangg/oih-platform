# 2026-03-18 Session Log

## 系统状态
- API: running (8080), 11容器全部up
- v4 pipeline 运行中：task_id 153a1638

## 今日完成

### 1. binder_design_pipeline 完整7步 ADC 链路
- AF3验证 Step3：top5 MPNN → AF3折叠 → ipTM分级过滤
- ADC Step4：freesasa偶联位点（SASA>40Å²）
- ADC Step5：linker_select（Cys→maleimide/Lys→NHS）
- ADC Step6：fetch_molecule MMAE from PubChem
- ADC Step7：rdkit_conjugate DAR=4建模
- pdb_id 参数支持（自动fetch_pdb）
- FASTA解析bug修复（_parse_mpnn_fasta）
- dry_run 支持

### 2. AF3 修复
- 超时改为动态无限等待（_wait_for_af3_task，每30s poll）
- 只有OOM/exit1/cancelled才判定失败
- _NO_DEGRADED_TOOLS = {"alphafold3","bindcraft"}
- AF3永不降级，等待GPU1空闲
- val_0实际ipTM=0.48（超时误判修复）

### 3. Task 持久化
- 每次状态变更写入 data/tasks/{task_id}.json
- 服务启动自动加载历史任务
- running/pending标记为failed（断电保护）

### 4. Cancel 接口
- DELETE /api/v1/tasks/{task_id}
- 前端取消按钮（左侧卡片+Results Hub详情）

### 5. Dashboard 大幅升级
- Results Hub：全工具历史结果，按工具分组
- 7种工具专业图表（Chart.js）：
  RFdiffusion柱状图 / MPNN折线图 / AF3散点+雷达 /
  GNINA水平柱+散点 / GROMACS双折线 / Chemprop雷达 / Pipeline漏斗+散点
- Protein 3D 闪烁修复（display:none/block永不卸载）
- cartoon模式VDW surface删除（大蛋白WebGL崩溃）
- 缩放/旋转/居中控制按钮
- 子任务折叠显示（不刷爆左侧列表）
- 静态文件路径修复（/outputs/挂载）
- 单输入框设计（合并Run/Send）
- Pipeline Preview（运行前显示流程步骤）

### 6. get_system_status 工具
- GET /api/v1/system/status
- 返回containers/gpu_queue/task_summary
- qwen_tools.py + qwen_agent.py TOOL_MAP + router三处注册
- Qwen可直接回答"有多少容器在运行"

### 7. 知识蒸馏系统
- skills/SELF_DIAGNOSIS_WORKFLOW.md（诊断手册）
- qwen_agent.py system prompt追加自主诊断指令
- data/distillation/ 目录，70条蒸馏数据
- scripts/collect_distillation_data.py（自动收集）
- 覆盖所有已知坑：GPU路由/AF3超时/GROMACS/容器规则等

## 已知问题
- AF3容器：JAX dtype兼容问题（"JAX has removed implicit dtype promotion"）
  → 需降级JAX版本，待修复
- v4 pipeline：AF3验证结果待查（预计明天完成）
- HER2 hotspot 可能需要换到Domain II新表位（ipTM偏低）

## 待完成
- [ ] JAX dtype bug修复（oih-alphafold3容器）
- [ ] v4 AF3结果验证
- [ ] bioRxiv Introduction写作
- [ ] 蒸馏数据积累到100条 → LoRA微调
- [ ] 虚拟筛选pipeline（2万PDBQT + AutoDock-GPU）
- [ ] Dashboard Pipeline Preview前端联调

## 蒸馏数据统计
- 手工案例：cases_2026-03-18.jsonl（7条）
- 历史提取：cases_from_history.jsonl（35条）
- 自动收集：auto_cases_20260318.jsonl（28条）
- 总计：70条

## 关键文件变更
- routers/pipeline.py：_wait_for_af3_task + 7步ADC pipeline
- core/task_manager.py：持久化+NO_DEGRADED_TOOLS+Cancel
- routers/system.py：get_system_status新增
- tool_definitions/qwen_tools.py：get_system_status工具+自主诊断prompt
- qwen_agent.py：TOOL_MAP+system prompt更新
- main.py：/outputs静态挂载+system router注册
- dashboard.html：全面升级（Chart.js图表+3D viewer修复+控制栏，~106KB）
- skills/SELF_DIAGNOSIS_WORKFLOW.md：新建
- skills_loader.py：self_diagnosis skill关键词映射
- data/distillation/：70条训练数据
- scripts/collect_distillation_data.py：自动收集脚本
