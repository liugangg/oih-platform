# 2026-03-21 Session Log

## 完成事项

### 1. DiscoTope3 完整集成（第14个容器）
- 3处注册（router/qwen_tools/TOOL_MAP）
- models.zip解压 + ESM-IF1(1.6GB)持久化到 /data/oih/model_cache/
- 验证：1N8Z HER2 → 195表位/1015残基，D560/E558/E561命中trastuzumab位点 ✅
- threshold默认0.5

### 2. IgFold 完整集成（第15个容器）
- 基于proteinmpnn:latest，igfold 0.4.0 + antiberty + abnumber
- 模型打包在pip包内，docker commit固定
- 验证：98残基nanobody，pLDDT=96.6，pRMSD=0.17Å，4.4s GPU ✅
- do_renum=False（ANARCI包名冲突 anarci vs anarcii，后续修复）

### 3. ESM2 突变扫描工具集成
- esm2_score_sequences：ESM2伪困惑度评分，验证 antibody PPL=1.78 ✅
- esm2_mutant_scan：ESM-1v 5位置扫描（CDR-H3），D90/T91/A92/V93/Y94 突变效应 ✅
- ESM-1v模型 7.3GB 下载完成并持久化
- oih-esm volume mount: /data/oih/model_cache/esm_torch_hub → /root/.cache/torch/hub
- 模型持久化：ESM2-650M(2.5GB) + ESM-1v(7.3GB) + contact-regression(3.7KB)
- TIMEOUT_ESM: 600→1800（ESM-1v首次加载慢）

### 4. IEDB + SAbDab接入RAG
- query-api.iedb.org PostgREST bcell_search endpoint
- SAbDab TSV全量下载 + antigen名过滤
- HER2查询：5条IEDB验证表位 + 1条curated + 2条PubMed = 8 results ✅
- asyncio.gather 30s总超时，PubMed efetch 10s+15s超时保护
- extract_antigen_name() 正则抗原名提取

### 5. pocket_guided_binder_pipeline 升级16步6D评分
- 新增epitope维度(0.20)：_compute_epitope_score_for_pocket() 口袋残基8Å内表位比例
- DiscoTope3与fpocket/P2Rank并行（step 3, asyncio.gather）
- known_epitope_override：DiscoTope3(score>0.7) ∩ RAG残基 ≥3 → 跳过scoring直接锁定hotspot
- 权重：p2rank(0.15) + sasa(0.15) + conservation(0.15) + rag(0.25) + electrostatics(0.10) + epitope(0.20)
- epitope_enriched：选中口袋附近的高分表位残基追加到hotspots（最多+3，上限8）
- HER2 2A91 验证：pocket 1 composite=0.372, hotspots A2/A270/A279/A280/A281/A282 ✅

### 6. Bug修复 & 系统维护
- BindCraft num_designs: 5→10（pydantic ge=10约束）
- pocket_guided_binder_pipeline + binder_design_pipeline + drug_discovery_pipeline 加入 _GPU_TOOLS(VRAM=20GB)
- oih-esm volume mount 持久化（之前无mount，模型重启丢失）
- oih-discotope3 volume mount（models + torch/hub）
- oih-igfold volume mount（/root/.cache）
- her2_6d_v1 pipeline 因重启中断于AF3 4/5（中间结果丢失）

## 当前平台状态
- **容器数**：15个（+discotope3 +igfold）
- **工具数**：29个（+igfold_predict +esm2_score_sequences +esm2_mutant_scan +discotope3_predict）
- **RAG源**：5个（PubMed + bioRxiv + IEDB + SAbDab + LocalDB 86 docs）
- **模型缓存**：
  - /data/oih/model_cache/esm_torch_hub/checkpoints/ — ESM2(2.5GB) + ESM-1v(7.3GB)
  - /data/oih/model_cache/torch/hub/checkpoints/ — ESM-IF1(1.6GB, DiscoTope3用)
  - /data/oih/model_cache/discotope3_models/ — 101个XGBoost JSON

## Pipeline 架构
```
          DiscoTope3 + IEDB + RAG → 表位发现
                      ↓
             known_epitope_override?
              ├── YES → 直接锁定hotspot
              └── NO  → 6D复合评分 + Qwen选择
                      ↓
              ┌───────┴────────┐
         RFdiffusion      BindCraft (并行)
              ↓                 ↓
         ProteinMPNN      内部AF2验证
              ↓                 ↓
         ESM2 PPL过滤     合并候选
         (PPL < 10)            ↓
              ↓           IgFold快速预测
         IgFold过滤       (pLDDT > 70)
         (pLDDT > 70)          ↓
              ↓           AF3精细验证
         AF3复合物验证     (ipTM ≥ 0.6)
         (ipTM ≥ 0.6)         ↓
              └───────┬────────┘
                  ADC装配
         (FreeSASA → Linker → MMAE → DAR=4)
```

## 待完成
1. HER2 her2_iedb_v1 端到端验证（known_epitope_override是否触发）
2. IgFold + ESM2 接入pipeline漏斗（ProteinMPNN→ESM2→IgFold→AF3）
3. dashboard pipeline preview更新为16步
4. 蒸馏数据集积累到100条→LoRA微调
5. docker buildx元数据损坏修复
6. BepiPred-3.0集成（与DiscoTope3双模型投票）
7. anarci包名修复（anarcii→anarci兼容层，启用CDR renumbering）

## Pipeline 60b4d10a Results (her2_override_v2)
- epitope_override_triggered: True (18 DT3×RAG matches, offset=+22)
- hotspots: A245, A246, A157, A258, A256, A286, A255, A260
- **RFdiffusion timed out at 3600s** — 18/20 PDBs generated before timeout
- BindCraft: 0 designs passed filters (exit 1, expected for hard targets)
- AF3: not reached (pipeline failed at step 8)
- Root cause: 8 scattered hotspot residues across multiple HER2 domains → slow diffusion
- Fix: limit override hotspots to top 6 most clustered residues

## Pipeline 17449435 Results (her2_clustered_v1) — 2026-03-21 20:37
- status: failed
- error: unsupported format string passed to NoneType.__format__
