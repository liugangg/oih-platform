# 2026-03-17 Session Log

## 系统状态
- API: running (8080), 11容器全部up
- 任务队列空闲

## 已完成

### 1. dashboard.html 配色方案修改
- 参考 NVIDIA 官网设计语言：深灰背景(#0f1117)，高对比度文字(#e2e8f0)
- 主背景 #0a0e1a → #0f1117
- 卡片/面板 rgba(14,20,38,...) → rgba(22,27,39,...)
- 边框 rgba(40,60,120,0.25-0.4) → rgba(255,255,255,0.08-0.12)
- 主文字 #c8d6e5 → #e2e8f0，次级文字 → #a0aec0，状态栏 → #94a3b8
- 输入框背景 → #1e2433，占位符 → #6b7280
- cyan/purple accent 完全未动

### 2. dashboard.html 输入框合并
- 删除顶部大输入框 + Run 按钮
- 保留 4 个 example prompt 快捷按钮，改为填入底部输入框
- 底部输入框 placeholder → "Describe your task or ask a question..."
- 按钮文字 → "Run"
- 清理废弃 state：input/setInput/inputRef/send()

### 3. binder_design_pipeline AF3 validation 完整实现
- Step 3 重写：top5 MPNN 序列 → AF3 复合物预测 → 解析 ipTM → 分级过滤
  - ipTM ≥ 0.75 → high confidence
  - 0.6 ≤ ipTM < 0.75 → low_confidence（降级通过）
  - ipTM < 0.6 → 不通过
- AF3 任务间隔 5 秒提交，避免 GPU OOM
- 单任务 timeout 1800 秒
- 结果按 ipTM 降序排列，重新分配 rank
- 输出结构：validated_designs[] + total_tested + passed + best_iptm + best_sequence
- BinderDesignPipelineRequest schema 扩展：
  - hotspot_residues: Optional[str] → Optional[list[str]]（自动 join 为逗号分隔传给 RFdiffusion）
  - 新增 num_designs（alias for num_rfdiffusion_designs）
  - 新增 dry_run: bool
- 新增 _make_dry_run_coroutine() 辅助函数
- dry_run 测试通过：返回 mock 结构，task completed

### 4. binder_design_pipeline Step 4-7: ADC 构建全流程
- Step 4 (FreeSASA): 取 best ipTM 的 AF3 结构 → freesasa 分析 → SASA>40Å² 的 Lys/Cys 位点 → top 3
- Step 5 (Linker Select): 根据位点类型自动选 maleimide(Cys) 或 NHS(Lys) → cleavable + compatible MMAE → top 1 linker
- Step 6 (Fetch Payload): PubChem 获取 MMAE SMILES + MW
- Step 7 (RDKit Conjugate): antibody_pdb + conjugation_site + linker_smiles + payload_smiles → ADC SMILES, DAR=4
- 每步独立 try/except，单步失败标记 `adc_design.partial=true`，不阻塞后续步骤
- AF3 输出 CIF → PDB 转换（for freesasa），fallback convert_af3_cif_to_pdb
- dry_run mock 含完整 `adc_design` 字段 + 7 个 step status
- dry_run 测试通过：`adc_design` 含 nanobody_sequence/iptm/conjugation_site/linker/payload/dar/adc_smiles/adc_structure_path
- Progress: Step 1-3 占 5-75%，Step 4-7 占 80-100%
- pipeline docstring 更新为 7-step

### 5. binder_design_pipeline 新增 pdb_id 支持
- `BinderDesignPipelineRequest` 新增 `pdb_id: Optional[str]`，`target_pdb` 改为 Optional
- `_run()` 开头加 Step 0: fetch_pdb（同 drug_discovery_pipeline 模式）
- Step 3 `_extract_sequence_from_pdb` 改用 resolved `target_pdb`（非 `req.target_pdb`）
- dry_run mock 含 `fetch_pdb` step
- 实际任务已提交：`HER2_nanobody_ADC_v1` (task_id: 53e8cba1)，pdb_id=1N8Z

### 6. Task 持久化 (core/task_manager.py)
- 每次 task 状态变更时写入 `/data/oih/oih-api/data/tasks/{task_id}.json`
- 服务启动时扫描目录加载所有历史任务（running/pending 标记为 failed）
- 写入字段：task_id, tool, status, progress, progress_msg, result, error, created_at, updated_at
- 验证通过：dry_run → 重启 → 任务仍可查询

### 7. Dashboard Pipeline Results tab
- 新增 `pipeline` tab：当 binder pipeline 完成时自动出现
- 6 个统计卡片：RFdiffusion / ProteinMPNN / AF3 / FreeSASA / Linker / ADC
- AF3 validated designs 表格：rank / confidence / ipTM / MPNN score / sequence
- ADC Assembly 摘要：nanobody / ipTM / site / linker / payload / DAR + 2D 结构
- 下载按钮组：Best PDB / Sequences FASTA / ADC Structure / Full Report JSON
- 错误展示：adc_design.error + partial 状态

### 8. Dashboard 3Dmol.js 修复
- fetch 返回非 200 时提前 return
- addModel 前验证 data 非空字符串
- addModel 独立 try/catch
- 外层 catch 加 ref.current 存在性检查

### 9. Pipeline FASTA 解析修复
- 新增 `_parse_mpnn_fasta()` 辅助函数：解析 MPNN FASTA 输出为 [{sequence, score}]
- Step 3 序列收集：先尝试 `sequences` 字段，fallback 到 FASTA 文件解析
- v1 因此 bug 导致 AF3 0/0，v3 已修复

### 10. HER2 nanobody ADC pipeline v3 已提交
- task_id: 6a34f32f-04b5-4062-94b6-6e0014506cb8
- pdb_id: 1N8Z, hotspot: S310/T311/Q313/L317, num_designs: 10
- 已启用持久化，重启不会丢失

### 11. Dashboard: Results Hub tab (功能1)
- 常驻 "Results Hub" tab（最右侧），按工具分组展示所有历史任务
- 左侧工具列表（200px）：工具名 + 任务数 badge + running 脉冲圆点 + 展开/折叠
- 右侧结果详情：根据工具类型渲染不同组件
  - docking → poses 表格，ADMET → predictions 表格，pdb_files → 文件列表
  - molecule → 2D 结构 + 属性，freesasa → 偶联位点，pipeline → 跳转提示
  - 通用 fallback → JSON pretty-print

### 12. Dashboard: Tab 高亮动画 (功能2)
- running 任务 → tab 边框 cyan 脉冲动画 (tab-running CSS)
- 任务完成 → tab 边框绿色 3 秒 (tab-done CSS)
- Tabs 组件增加 runningTools/recentDone props
- poll 中跟踪 running→completed 转换

### 13. Dashboard: Pipeline Preview (功能3)
- 输入框有内容时前端关键词匹配显示流程预览
- 6 种 pipeline 模式：binder+ADC / drug discovery / docking / screening / MD / ADMET
- 横向流程图：emoji + 工具名 + 预计时间，→ 箭头连接
- 可折叠（× 关闭），发送后自动 reset
- 不调用后端，纯前端 regex 匹配

## 待完成
- [ ] Case 2：纳米抗体设计实验（Fig 4 数据）
- [ ] Case 3：HER2-ADC 设计实验（Fig 5 数据）
- [ ] Fig 6：各工具运行时间统计
- [ ] MD 动画渲染（MDAnalysis → gif）
- [ ] 100ns MD 从 22.74ns checkpoint 续跑
- [ ] MM/PBSA 结合自由能计算
- [ ] 端到端真实任务测试：dashboard 动态 Tab 解锁验证
