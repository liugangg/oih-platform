# 2026-03-22 Session Log

## 完成事项

### 1. NoneType format string bug 修复
- 根因：`known_epitope_override` 触发时 `composite_score=None`，但 `f"{sel.get('composite_score', 0):.3f}"` 返回 None（key 存在时 .get() 不用 default）
- 修复：`(sel.get('composite_score') or 0)` — None 安全
- 同时修复：`ig_result.get("mean_plddt") or 0` — 防御 IgFold 返回 None pLDDT
- ESM filter timeout: 300s → 1800s（ESM-1v 7.3GB 模型加载慢）

### 2. Two-Tier Hotspot Identification 架构升级（第30个工具）
- **新工具 `extract_interface_residues`**：从已知复合物 PDB 提取抗体-抗原界面残基
  - 注册位置：routers/structure_prediction.py, qwen_tools.py, qwen_agent.py TOOL_MAP
  - 实现：BioPython NeighborSearch（oih-discotope3 容器内执行）
  - CPU-only，~1秒，加入 _CPU_TOOLS

- **Target Tier Classification**：`_classify_target_tier()` 函数
  - Tier 1: 已知抗体复合物 → `extract_interface_residues` → 实验验证 hotspots
  - Tier 2: RAG 发现同源结构 → rag_guided
  - Tier 3: 新靶标 → DiscoTope3 + IEDB + RAG → 计算预测

- **KNOWN_COMPLEXES 注册表**：HER2, PD-L1, EGFR, VEGF, CD20, TNF

- **Pipeline 集成**：Tier 1 跳过 steps 3-6，链 ID 自动映射

- **Schema 更新**：PocketGuidedBinderPipelineRequest 新增 `target_name`, `antigen_chain`, `binder_type`

### 3. binder_type 参数与 IgFold 跳过逻辑
- IgFold 只适用于抗体/纳米抗体序列（AntiBERTy 训练）
- de novo binder（RFdiffusion 设计）→ pLDDT 无意义（实测 0.4-12.6）
- `binder_type='de_novo'` 时跳过 IgFold，ESM2 过滤后直接 AF3
- 新建 skills/TOOL_SCOPE_RULES.md + skills/IGFOLD_WORKFLOW.md

### 4. DOMAIN_REGISTRY + _get_af3_antigen_regions()
- 通用抗原域截取：按 UniProt/Pfam 结构域边界截取，padding=30aa
- 已注册：HER2 (domain2+domain4), CD36 (extracellular), EGFR (domain1+domain3), PD-L1 (IgV)
- binder_design_pipeline AF3 改为多域验证，`num_seeds=3`，取最高 ipTM
- 未知蛋白以 hotspot 中心 ±100aa 截取

### 5. Publication Figures 生成（4 张 Nature Methods 风格）
- fig1_platform.pdf: 工具生态 + 16 步 pipeline 流程图
- fig2_her2_case.pdf: DiscoTope3 表位 + 6D 评分 + ipTM 进展
- fig3_agent_decision.pdf: 表位 override 决策逻辑 + hotspot 空间聚类
- fig4_funnel.pdf: 多级候选筛选漏斗
- 300dpi PNG + 矢量 PDF，嵌入字体，colorblind-friendly

### 6. Git 仓库初始化
- `git init` + branch=main + 69 files tracked
- 硬编码 IP 全部替换为 `OIH_SERVER_HOST` 环境变量
- .gitignore: 排除 tasks/, outputs/, __pycache__/, .env
- README.md: 完整平台文档
- tag: v0.1.0-pre-results

### 7. RDKit ADC 3D embedding 修复
- 大分子（ADC linker-payload）EmbedMolecule 失败 → 不再报错
- fallback 返回 `embedding_status="2d_only"` + adc_smiles（SMILES 字符串）
- 修复 ETKDGv3 参数传递（useRandomCoords 移到 params 对象）

### 8. Skills & 文档同步
- CLAUDE.md: Target Tier System + DOMAIN_REGISTRY 章节
- skills/DISCOTOPE3_WORKFLOW.md: Tier 分类 + 适用范围限制
- skills/IGFOLD_WORKFLOW.md: 完整工作流 + 适用范围限制
- skills/TOOL_SCOPE_RULES.md: 全工具适用范围表
- skills_loader.py: igfold + tool_scope 关键词映射
- qwen_tools.py: 工具适用范围规则注入 Qwen 系统提示

## HER2 Tier 1 Case Study 结果

### her2_tier1_v2 (74fc3aa3) — Tier 1 路径验证

| 步骤 | 结果 |
|------|------|
| Tier 分类 | Tier 1: HER2 → 1N8Z (trastuzumab) |
| Interface 提取 | C558,C571,C572,C573,C603,C560,C570,C557 (Domain IV) |
| Steps 3-6 | 全部跳过 (tier_1) |
| RFdiffusion | 10 backbones (聚集 Domain IV hotspots) |
| ProteinMPNN | 50 sequences |
| ESM2 filter | 50/50 pass (best PPL=4.40) |
| IgFold | 跳过 (binder_type=de_novo) |
| **AF3 val_0** | **ipTM = 0.84** ✅ (项目最高分) |
| **AF3 val_1** | **ipTM = 0.70** ✅ |
| AF3 val_2~9 | 中断（服务重启） |
| FreeSASA | Chain A: K107(102.8Å²), K103(94.3Å²), K205(73.7Å²) |
| Linker | MC-VC-PABC (maleimide_thiol, cathepsin B cleavable) |
| RDKit | 3D embedding failed → 修复为 2D SMILES fallback |

### Tier 1 vs Tier 3 对比

| 指标 | Tier 3 (her2_final_v1) | Tier 1 (her2_tier1_v2) |
|------|----------------------|----------------------|
| Hotspot 来源 | DiscoTope3 + RAG (分散) | 1N8Z 界面 (Domain IV 聚集) |
| IgFold | pLDDT=5.3 (0/30 pass) | 跳过 (de_novo) |
| AF3 best ipTM | 未到达 | **0.84** |
| 步骤跳过 | 0 | 4 步 (更快) |

## 当前平台状态
- **容器数**：15 个
- **工具数**：30 个
- **RAG 源**：5 个
- **KNOWN_COMPLEXES**：7 个靶标
- **DOMAIN_REGISTRY**：4 个靶标 (HER2, CD36, EGFR, PD-L1)
- **Git**: main branch, 2 commits, tag v0.1.0-pre-results

## 关键陷阱（血泪经验）
1. IgFold 只适用于抗体序列 — de novo binder 的 pLDDT 无意义（0.4-12.6）
2. FreeSASA 不接受 CIF — 需先用 BioPython MMCIFParser 转 PDB
3. RDKit EmbedMolecule 对大分子 ADC 失败 — 需 2D SMILES fallback
4. .get() 当 key 存在但 value=None 时返回 None（不是 default）— 用 `or 0`
5. linker_library 没有 NHS-amine 类型 — 需补充
6. 服务重启会丢失 running pipeline 状态 — AF3 子任务在容器内继续但编排丢失

## 待完成
1. ☐ 重新跑 val_2~val_9 的 AF3 验证（8 个设计）
2. ☐ Fig2 Panel C 更新 v4 真实 ipTM 数据 (0.84)
3. ☐ CD36 Tier3 pipeline（PDB 5LGD）
4. ☐ pocket_scoring_log.jsonl 更新权重学习数据
5. ☐ GitHub push（等全部 AF3 结果）
6. ☐ linker_library 补充 NHS-amine 类型
7. ☐ Dashboard pipeline preview 更新为 tier system
8. ☐ Tier 2 实现（同源结构 transfer）
9. ☐ 蒸馏数据集积累 100 条 → LoRA 微调
