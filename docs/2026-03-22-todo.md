# 2026-03-22 待办事项

## 优先级 P0（必须完成）

### 1. 检查 her2_clustered_v1 (17449435) 结果
- 查看 `/tmp/pipeline_17449435_result.txt`
- 如果成功：提取 AF3 best_iptm，保存 winning design PDB
- 如果失败：诊断原因，调整参数重提

### 2. IgFold/ESM2 漏斗端到端验证
- 上次 pipeline 用的是旧代码（ESM2+IgFold 漏斗在 restart 后才生效）
- 需要重启后提交一次，观察 ESM filter + IgFold filter 是否正常工作
- 预期：100 MPNN seqs → ESM2 PPL<15 过滤 → ~30 → IgFold pLDDT>70 → ~10 → AF3

### 3. RFdiffusion 大靶标性能问题
- HER2 ECD 506 残基，每设计 20-40 min
- 考虑：只用 ECD domain（残基 1-200）做 target，不用完整 506 残基
- 或者：用 `chains="A"` + `trim_target` 参数只保留 hotspot 附近 ±30 残基

## 优先级 P1

### 4. BindCraft 容器诊断
- 目前 BindCraft 对 HER2 0 设计通过 AF2 过滤（exit 1）
- 检查 trajectory_stats.csv 和 failure_csv.csv
- 可能需要放宽 BindCraft 内部过滤条件（advanced_settings）

### 5. 蒸馏数据集
- 当前 73 条，目标 100 条
- 收集今天 12 个 bug fix 案例 → 85+ 条
- 100 条后启动 LoRA 微调实验

### 6. dashboard 更新
- pipeline preview 已更新为 16 步（已做）
- Results Hub 需要显示 pipeline 运行历史表
- 添加 ESM2/IgFold 过滤结果可视化

## 优先级 P2

### 7. docker buildx 元数据损坏修复
- `failed size validation: 1383 != 1269` — 影响所有 docker build
- 当前 workaround：docker run + pip install + docker commit
- 根治：清理 buildx cache 或重装 buildx

### 8. BepiPred-3.0 集成
- 与 DiscoTope3 双模型投票，提高表位预测可靠性
- 需要新容器

### 9. anarci 包名修复
- IgFold 用 `anarci`（旧包），pip 安装的是 `anarcii`（新包）
- 需要 symlink 或 monkey-patch 让 IgFold 能用 anarcii
- 启用后 CDR renumbering 可用

### 10. ESM 模型缓存 volume mount 生效验证
- oih-esm 需要 docker compose restart 才能激活 volume mount
- 当前 models 在容器内 + 宿主机都有（双份保险）
- 验证：restart 后 ESM2 加载不重新下载

## 系统状态（2026-03-21 EOD）
- **容器**：15 个 running
- **工具**：29 个 registered
- **RAG**：5 源（PubMed + bioRxiv + IEDB + SAbDab + LocalDB 86 docs）
- **模型缓存**：ESM2(2.5GB) + ESM-1v(7.3GB) + ESM-IF1(1.6GB) + DT3-XGBoost(101)
- **Pipeline**：16 步 6D 评分 + known_epitope_override + ESM2/IgFold 漏斗
- **待验证**：her2_clustered_v1 (17449435) 运行中

## 关键文件
- 维修记录：`docs/2026-03-21-maintenance.md`
- Session log：`docs/2026-03-21-session.md`
- DiscoTope3 workflow：`skills/DISCOTOPE3_WORKFLOW.md`
- Pipeline 代码：`routers/pipeline.py`（pocket_guided_binder_pipeline）
